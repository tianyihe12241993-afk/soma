# SOMA miner registry — OUR hotkeys (subnet 114).
#
# ⚠ 2026-07-07: the entries below are the comp-107 research set (historical). The comp-108
# assets (np2 5CPbtf… / np3 5F9ZRe… / CHAMPION cap32+pin 5DAbJik… = upload_miner_uphard_salience.py
# sha256 695b4fe4) were tracked in state/ on the comp-108 machine and never merged here.
# comp-110 (CoT-Compression-5, uploads close 13 Jul 14:30 UTC): NO hotkeys assigned yet.
# ON UPLOAD (USER-run): add each comp-110 submission here AS PART OF THE UPLOAD STEP —
#   cN: { hotkey: <SS58>, label: <name>, version: <file+sha256>, notes: "comp-110 <what/why>" }
# Scores stay hotkey-anchored; hotkey→file mapping recorded at upload time is what makes
# scored reads auditable (the comp-108 label-audit lesson).
#
# (below: competition 107 registry — COMPLETED comp.)
# hotkey→version mapping AUDITED 2026-06-21 (reports/label_mapping_audit.md): the
# registry stands (m9=v22, m10=v18); the conflicting chat label was rejected.
# Per-hotkey scores are scraped from the dashboard (source=platform). The derived
# stats (avg_ratio / broke_baseline / negative_score / barely_compressed) are
# computed from the immutable per-task scrapes in data/raw/dashboard/*_miner_detail.json
# — they are DERIVED, not hand-entered. Flow-style one-liners so stdlib parses them.
competition_id: 107
netuid: 114
wallet: tony-miner

# ---- focal research set (the 5 submissions under study) ----
miners:
  m7:  { hotkey: 5GsHHNmexGGkrdjZPHW5jyCcgfUodazHeEhPBx4Rpxmy17KB, label: m7,  version: v11.1, total: 1.279, easy: 1.127, medium: 1.421, hard: 1.281, avg_ratio: 2.99, broke_baseline: 1, negative_score: 3, barely_compressed: 8,  notes: "BEST confirmed. m7-style harvest. Protect/replicate. 1 fragile task (sympy-17139)." }
  m8:  { hotkey: 5EF8nHthtDV3woo6oK2rNk3vGJ8ag6X1fzPMJuLhAZAMjmtc, label: m8,  version: v15,   total: 1.159, easy: 1.057, medium: 1.183, hard: 1.230, avg_ratio: 2.84, broke_baseline: 0, negative_score: 2, barely_compressed: 10, notes: "persistent-fail routing; Medium leaked 1.421->1.183. Net negative vs m7." }
  m9:  { hotkey: 5CFqU2SsJbZjGwWS8cwjcCWrXxEo7gEpsE76SagDCihCmsMY, label: m9,  version: v22,   total: 0.937, easy: 1.165, medium: 0.931, hard: 0.728, avg_ratio: 2.64, broke_baseline: 3, negative_score: 9, barely_compressed: 14, notes: "flip-richness + release-on-pass. WORST. 3 broken baselines, 9 neg tasks. Drop flip." }
  m10: { hotkey: 5GL4KxdashDdVd7twAGNoooPwTdkoxmcYKiEMiUBAQQaJKPh, label: m10, version: v18,   total: 0.916, easy: 1.029, medium: 1.073, hard: 0.653, avg_ratio: 2.66, broke_baseline: 2, negative_score: 8, barely_compressed: 15, notes: "flip-richness (subset of v22). Worst Hard 0.653. Drop flip." }
  m11: { hotkey: 5F26J6aGytyLoCT9T4EXqj6BNwrTJEmLtF3XsAXE436SmtTE, label: m11, version: v24,   total: 1.038, easy: 0.943, medium: 1.030, hard: 1.135, avg_ratio: 2.70, broke_baseline: 1, negative_score: 7, barely_compressed: 12, notes: "flip + firm-stop decisiveness. Best of the flip bets but still < m7." }
  # --- NEW ROUND (CoT-Compression-4 / comp 108) ---
  m12: { hotkey: 5Dz7JaCBw6t9ktdRVaLYi3ntEzvCDfb5XmTHyzPdDyT6KS9j, label: m12, version: m7-compliant, competition: 108, key: "sk-or-...1e8a (current)", sha256: 3c4e3086, uploaded: "2026-06-22 15:52 local", status: "SCORED 0.768 (E0.412/M0.953/H0.919), #2, review-PASS — LIVE/BEST", notes: "FIRST new-round submission. m7 engine, coach/force-stop/digest-injection REMOVED, only [[CMP]]/[[BLOCK N]]/loop_detected markers. Review-safe baseline. solution=miner/cot_compression/upload_miner_m7_compliant.py" }
  m13: { hotkey: 5CApy5s5NvzuaxgGnKiBmGpwtqvTFysjzdHHiPiwxwxdwEFz, label: m13, version: m12.1b_run_stability, competition: 108, key: "sk-or-...1e8a (same as m12)", sha256: 90460bb9, uploaded: "2026-06-23 11:04 UTC", status: "SCORED 0.571 (E0.561/M0.718/H0.434), review-PASS — WORSE than m12, REJECTED", notes: "m12 + never-inflate + aggressiveness-cap(>8x harvest->rich) + deterministic + tightened routing. PLATFORM FALSIFIED the design: H 0.919->0.434 + M 0.953->0.718 COLLAPSED (cap/gentling traded away winning harvest), only Easy rose 0.412->0.561. Local eval MISLED. m12 stays live. solution=upload_miner_m12_1b.py" }
  m14: { hotkey: 5DRi5yU6ZVuzJpTGsfdewMcMxz1t3wTbirwLd3Z6bZymiDSD, label: m14, version: m14_never_inflate_only, competition: 108, key: "sk-or-...aa828 (NEW key — CONFOUND)", sha256: 141f7352, uploaded: "2026-06-23 ~13:4x UTC", status: "SCORED 0.423 (E0.399/M0.537/H0.341) — CONFOUNDED by NEW key/backend, INVALID for never-inflate", notes: "Same code as m15 but DIFFERENT key -> weaker provider backend -> uniform pass 64%->51% collapse. NOT a never-inflate verdict. Superseded by m15 (clean re-test). solution=upload_miner_m14.py" }
  m15: { hotkey: 5EX5r3URph5cwiLxh5GeYXbysFSA7we8rKUZKzL711DMPLi9, label: m15, version: m14_never_inflate_only, competition: 108, key: "m12/m13 lineage (verify-or-fallback to SOMA-benchmark/.env; suffix pending user confirm)", sha256: 141f7352, uploaded: "2026-06-23 ~15:5x UTC", status: "uploaded — SCREENING (clean A/B re-test of m14 code)", notes: "EXACT m14 code (sha 141f7352) re-submitted with the PROVEN m12/m13 key lineage (the m14->aa828 key was the confound). Clean A/B: only intended diff vs m12 = never-inflate guard. Accept over m12 only if review-PASS + Easy>= & M/H~m12 + NO broad pass-rate collapse. m12 stays LIVE. solution=upload_miner_m14.py" }
  m21sub: { hotkey: 5ENtgEWhp3ZGW3sTMBtxUfxs9QvN3boH15TnQpE8foqa1BFu, label: m21, version: m12_keepmore_hybrid, competition: 108, key: "same OpenRouter account as m12", sha256: 79ee3839, uploaded: "2026-06-25 ~04:1x UTC", status: "SCORED 0.597 REJECTED (E0.564/M0.766/H0.461). DIAGNOSED (wxx45mc3v): keep-more was NEUTRALIZED by its own savings-floor guard (falls back to m12-equiv on ~all rewrite-model harvest turns -> byte-identical m12 -> tokens_with~=m12); Hard crater came from the narrow keep-more-activation window poisoning state + tipping into rich early with a bigger trajectory. tokens_without 5x = native-session-balloon ARTIFACT not better compression. CONCLUSION: keep-more is HARD-INCOMPATIBLE (harvest feeds rich via state). Original (wrong) note: Hard CRATERED 0.919->0.461. BUT Easy lever VALIDATED: on confirmed-Easy set m12 +0.462->m21 +0.876 (+0.414), fixed exactly the predicted breaks (sympy-14531 -0.16->+1.78, sympy-14976 -0.76->+1.04, sympy-11618 +0.04->+1.29, sympy-20590 +1.21->+2.20). The SAME lightening that fixes Easy breaks CRATERS Hard (Hard transits harvest; m18/AOW failure mode, 3rd confirmation). m12 LIVE untouched = our 4.8% SAFE. solution=upload_miner_m21.py", notes: "KEEP-MORE hybrid: LIGHTER harvest (PRESSURE_RELIEF 0.75->1.0, TARGET 8k->11k, KEEP_RECENT 4->6 = ~25% global savings, +5pp over floor) + never-drop RETENTION class (is_retention_critical, CALL-only: source-edit/test-file calls never dropped = the whole-interaction-drop fix m17 couldnt do) + extractive-then-cap + pure never-inflate floor guard. HARVEST-ONLY; rich/compress_gently/handle_assemble BYTE-IDENTICAL to m12 (Hard 0.919 provably safe, verified). Smoke PASS: keeps MORE (+20k chars/+6 msgs/3 fewer drops) yet 82.4% savings (bounded), Hard byte-identical, never-inflate, deterministic, worst-case==m12. CAVEAT: headroom predicted BELOW the 0.25 measurability gate / 0.32 scrape noise -> NOT predicted to clearly beat m12; zero-downside probe. GATE: read only when scored; first signal=screener (does it stop breaking django-11551, the task that DQd m17?); promote ONLY if >=3 scrapes show noise-clearing Easy/Medium gain + Hard>=0.919. USER signed off on the per-turn savings-floor reconciliation. m12 (5Dz7) stays LIVE. solution=upload_miner_m21.py" }
  m22sub: { hotkey: 5CffmtHw6Ug2voLWV6KXeJJzCkEPzDDvkCgtjffrFd9AJiG7, label: m22, version: m12_extractive_constant_budget, competition: 108, key: "sk-or-...c7e299 (NEW key, SAME OpenRouter account as m12 — USER-CONFIRMED, no backend confound; full key only in config/secrets.env, gitignored)", sha256: aeccd26e, uploaded: "2026-06-25 ~16:2x UTC", status: "SCORED 0.672 (E0.712/M0.742/H0.564) [label still evaluating but all 250 runs in = final]. REJECTED (< m12 0.768). EASY +0.300 (WORKED - break-fixes -> all-5-consistent-pass on django-14122/11740/11095/12754/13033/11551). BUT Medium -0.211, Hard -0.355: the extractive selector KILLED FLIPS (django-13925 3->0, django-14017 3->0, sympy-18698 3->1) + NEW breaks on passing tasks (django-12774 pass->4brk, django-15375). 3rd Hard crater (m18/m21/m22) - tokens<=m12 invariant protected token COUNT but NOT content; content-change still craters Hard/flips. TRADE: break-fix selection kills flips (break-fix wants the error span, flip wants the exploration). Net -0.096.", notes: "m12 + EXTRACTIVE-not-blind harvest at CONSTANT byte+TOKEN budget. CHANGE1: kept results extractive-then-cap (m17 mechanism). CHANGE2: would-be-DROPPED interactions keep a TINY critical span (DROP_SPAN_CAP 600: changed-hunk+traceback+error+sig) instead of dropping to zero - the part m17 MISSED. CHANGE3: 5-rung byte-reconciliation ladder ships first rung with chars<=m12 AND tokens<=m12 (final_token_estimate=real tiktoken on platform); rung4=m12-exact fallback. +Hard-ceiling(2M char) +collapse-guard, both only reduce. rich/compress_gently/passthrough/router BYTE-IDENTICAL to m12. VERIFIED: gates PASS post-fix (worst m22/m12 token ratio 1.000, 0 breaches; 166/250 turns ship rung1 span-retaining + token-safe, 84 fall back m12-exact -> VALUE NOT neutralized; rich byte-identical; compliant; deterministic). Adversarial review CAUGHT char-vs-token bug (extractive fragmentation tokenizes higher at equal chars = m21-crater class) -> FIXED to gate on tokens too. EXPECTED: Easy 0.398->~0.763, Hard 0.919 PRESERVED (tokens<=m12 invariant), Medium ~unchanged -> flips Pair(E,H) (E+H 1.659>5DtEz 1.650) + keeps Single-H = ~14.3% share (from 4.8%). CAVEAT: token figs are BPE proxy (no tiktoken in sandbox, py3.14); correct by construction (gates on final_token_estimate); platform is real test. Distinct from m17(truncation-only,DQd screener)+m21(kept-more->coupling->Hard crater). GATE: screener-pass + >=3 scrapes Easy gain w/ Hard>=0.919. m12(5Dz7) LIVE. solution=upload_miner_m22.py" }
  m25sub: { hotkey: 5GpB36LUSTWKuL6DeYwoKrwQXSDMDBCw6zoEzQmREV8DMHy9, label: m25, platform_label: m23, version: m12_gated_gentle_breakfix_decoupled, competition: 108, key: "NEW key (user 'new key added' 2026-06-26) — ⚠ MUST confirm SAME OpenRouter account as m12 (…1e8a lineage); if a DIFFERENT account => CONFOUNDED score (m14 lesson, false 0.423). full key only in config/secrets.env", sha256: 52fe47f9, uploaded: "2026-06-26 ~01:0x UTC — platform hotkey LABEL = m23 (NOTE: the m25 CODE; the old 'm23' was a NO-GO design never built, no conflict)", status: "SCORED 2026-06-26: E0.859/M0.667/H0.536/tot0.684, ZERO penalty (clean). FAILED as m12-replacement (Hard 0.536<<0.919, gate failed) - m12 stays LIVE. BUT Easy 0.412->0.859 (+0.447) = #2 in FIELD (behind 5GCWaCnb 0.923, above king 0.858) -> m25 is #2 on Single-E (+0.064 to win). MECHANISM (dual-agent): extractive swap = high-variance PERTURBATION; Easy gain ~86pct pass-pass boost (perturbation +6.09 of +7.05) +14pct ratio +little break-fix; STRICTLY anti-flip (0 new flips, killed 3) + destabilizes 18 passing tasks (-10.6) = why M/H cratered. Confirmed the Codex pre-upload audit + m23 prediction. m25 = proof-of-concept EASY SPECIALIST (not a replacement). solution=upload_miner_m25.py. Reports: m25_build.md (+Codex audit).", notes: "m12 + 3 surgical HARVEST-only changes: (1) GENTLE break-fix = extractive-then-cap at TRUNCATION sites only (m17 mechanism), NO drop-spans (m22's CHANGE2 omitted as the bigger destabilizer); (2) RESOLVED-GATE = break-fix fires ONLY on settled turns (recent_errors==0 over window 6, not still_failing, not escalated, depth<80, >=3 results, prev!=rich); (3) DECOUPLED STATE = emit break-fix but SAVE m12-exact to state so source-match feeds rich/Hard clean (hedges m22 deep-Hard state-drift crater). Reconciliation ships break-fix only if <=m12 tokens AND chars; else m12-exact. Phase-A PASS: 54 fns byte-identical to m12 (incl compress_gently/rich, loop guard); rich+passthrough+gate-off outputs byte-identical to m12; gate-on <=m12 tokens+chars + state saves clean (decoupled); deterministic; compliant (only allowed markers); m12 file untouched/git-clean. Real-traj profile (15 comp-108 m12 trajs): ACTIVE not inert (ships 545x), targets Easy/Medium (286/250 ships, 32/43 content-changes), Hard well-protected (gate fires 16/2315, changes 6); RESIDUAL FLIP-LEAK = 240 gate-fires/201 ships on flips (decouple protects DEEP flips via rich-reads-clean, NOT shallow flips -> the coin-flip risk). Strictly safer than m22 (gate skips active-failing turns; deep flips protected) but NOT flip-clean. ACCEPT GATE (>=3 scrapes): Medium UP AND Hard>=0.919 AND flips>=m12; else reject. Local replay faithful for routing/ship/leak but CANNOT predict break OUTCOMES (fresh-sample variance) - platform is the test. m12(5Dz7) LIVE/untouched. Reports: reports/m25_build.md; harness /tmp/m25_safety.py. solution=upload_miner_m25.py" }
  m26sub: { hotkey: 5EkcybHjWyP3Pi26Hr2FYsCNyVKHEDEyDC9abaJoheQDBgFP, label: m26, version: np1_near_passthrough_cachestable, competition: 108, key: "SEPARATE hotkey (user-confirmed 5Ekcy); SHOULD be SAME OpenRouter account as m12 — CONFIRM no backend confound (m14 lesson: a different acct = false low score)", sha256: 06d15887, uploaded: "2026-06-26 ~16:0x UTC", status: "UPLOADED — screening/in-queue. READ ONLY WHEN status=scored. FIRST candidate for the WEIGHTED-TOKEN regime (commit b79fcaee).", notes: "STATELESS near-passthrough cache-stable miner. handle_assemble = ONE deterministic idempotent per-message cap (cap_tool_result: extractive-cap each toolResult >6000 chars, active=frozenset = PURE fn of own content, wrap [[CMP]] ONLY if it strictly shrinks = no-inflation invariant per Codex audit; passthrough everything else incl already-[[CMP]]/<=cap). NO save_state / resolve_stateful_messages / mode-routing -> the aow_bet/m21/m22 cross-mode state-coupling Hard-crater CANNOT occur (one mode). Reuses ALL m12 compliant primitives; m12 untouched/git-clean. OFFLINE-VALIDATED (Claude+Codex, reports/cache_stable_design.md §10-11): prefix-stability 100%/min99.8% (vs m12 70%/min5.8, aow_bet 99.9); idempotent (np1 on own output byte-identical); output<=native ALWAYS (no-inflation fixed+revalidated 6001->6001); 0 orphans/80 turns; compliant (allowed markers only, no LLM/task-ID/network); passthrough when small (changed=False native). Codex pre-upload audit: compliance/idempotence/window CONFIRM, no-corruption=accepted WARN (string-only on compressed results, m12-equiv), no-inflation NO-GO->FIXED. OPEN (platform-only): pass-rate (6k cap break agent?) + savings-floor (under-compression penalty ~0.70 like 5DCnA57?). EXPECTED band m12 0.130 < np1 <= ~0.697; >> our best m25 0.449. GATE: if it eats the savings penalty, tune NP_RESULT_CAP=6000 DOWN. m12 (5Dz7) stays LIVE. solution=upload_miner_np1.py" }
  np2sub: { hotkey: 5CPbtf3sUKrpGikpCuDVvMrpTe4VpPWMXNX5sFeTXHDSY7QS, label: np2, version: np2_near_passthrough_lightcap16k, competition: 108, key: "SEPARATE hotkey (5CPbtf); SHOULD be SAME OpenRouter account as m12/m26 — CONFIRM (m14 confound)", sha256: a64231c9, uploaded: "2026-06-26 ~19:40 UTC", status: "SCORED #2 of 64: total 0.684 (E1.052/M0.849/H0.173). WINS Pair(E,M) 0.951 + Single-M 0.849 = 14.3% OF THE POOL (from m12's 0% post-regime). OUR LIVE BEST. Thesis confirmed: lighter cap 6k->16k recovered Medium 0.620->0.849 (+0.229), Easy held ~tied#1, NO under-compression penalty. Gap to grow = HARD (H0.173). dissect: cache 93%, ratio 1.49x, 11 neg (wander + biggest Hard tasks 315@2.57x/296@3.27x still over-compressed).", notes: "np1/m26 + NP_RESULT_CAP 6000->16000 (the ONLY change). Pre-check (scripts/ratio_compare.py): passes through results <=16k (ratio 1.0x, NO inflation — guard holds), compresses only >16k; keeps the break-task results (m26 broke 315@2.43x/297@2.86x, ~16-18k results) ~full -> should FIX m26's 2 over-compression breaks; but near-passthrough on TYPICAL tasks -> forgoes savings (under-compression-penalty risk, platform-only; 5DCnA57@1.18x clears it so borderline). Codex pre-upload audit (agent ad30a0a): SAFE — only the cap changed; no-inflation/idempotence/compliance all hold; lighter-cap = empirical CALIBRATION bet. Offline-validated: 99.9% prefix-stable, idempotent, <=native, compliant. GATE on scored: total vs m26 0.610 + #neg (did the 2 breaks get fixed?) + cache% + did it under-compress. WIN -> new best/contend #1; UNDER-COMPRESS -> flat-cap is binary (cant hit 5DCnA57's uniform 1.18x), pivot to cache-stable LOSSLESS DEDUP (the real uniform-light lever). m26(np1) stays LIVE as #3; m12 LIVE/untouched. solution=upload_miner_np2.py" }
  streliefsub: { hotkey: 5DLMDwMmZd9g9w4MjkwFUFj8i8ZQq1MyCnb4ms8mTkKHh6zZ, label: strelief, version: np2_adaptive_breakprone_relief16k_28k, competition: 108, key: "DeepInfra-pinned account (same as np2/np3/m26). VERIFY Easy~1.05 binding at scored FIRST.", sha256: c70bd40a, uploaded: "2026-06-28 ~20:4x UTC", status: "SCORED 0.509 — DEAD (clean fail). Relief on failure-output -> the model WANDERS (7 breaks), did NOT flip Hard. The Hard-flip-via-relief idea is CLOSED. DO NOT re-run as strelief; hotkey 5DLMDwMm is now a reusable DEAD slot for a clean np2/np3 variance draw.", notes: "ADAPTIVE cap = np2 16k on normal results + np3's 28k RELIEF on FAILURE-OUTPUT-bearing results (real Traceback / pytest ===FAILURES=== / ^E + spaces / ^FAILED ::). Combines np2 Easy-savings + np3 break-recovery (np3 recovers np2's break-prone tasks best, +0.60 vs np2 -0.30). Targets the kings' break-avoidance edge. CAVEAT (Codex agent ad5164fc): the kings' edge over np2 (0.044) is WITHIN VARIANCE (Z~0.66 SE; per-task noise floor 0.66) -> strelief targets a within-noise gap; a ~np2-or-slightly-above score is NOT proof, read carefully. RISK: failure-signal must catch np2's actual break-prone results (partial coverage). ADDITIVE: np2/np3/m26 hold 14.3% regardless. AT scored: Easy~1.05 binding FIRST; then did np2's break tasks (268/293/297/315) recover toward np3 while E/M held? solution=upload_miner_strelief.py" }
  np2d1sub: { hotkey: 5FPPav7s158D2smJ7q7GW87HERDgz1K4krpdeb66gqZaVThP, label: np2d1, version: np2_byte_identical_variance_draw1, competition: 108, key: "MUST be its OWN funded account on DeepInfra+Venice (isolated -> no contention; the m33 crater rule). VERIFY Easy>=0.85 + tokens>=0.9x np2 at scored else STARVED=discard.", sha256: a64231c9, uploaded: "2026-06-29 ~04:33 UTC", status: "SCORED 0.4896 = BAD DRAW -> DISCARD. CORRECTED diagnosis (real per-run via vet_draw.py): the ACCOUNT was FINE (cache 93.1% = DeepInfra, ~= np2-ORIG's 93.9%; NOT a weak provider as first thought). It was an UNLUCKY VARIANCE draw: break 15.3% vs np2-ORIG 10.6% on BYTE-IDENTICAL code (+1 timeout). Confirms 0.684 is np2's FAVORABLE draw, NOT a floor (typical clean np2 draw ~0.55-0.6). ACTION: discard the 0.49; the account is OK to REDRAW on (no routing fix needed). Always vet first: `python scripts/vet_draw.py --hotkey HK` (CLEAN = cache>=88% & break<=13%).", notes: "VARIANCE DRAW #1 of the best-of-N plan (reports/variance_draw_plan.md). TARGETS at scored: PRIMARY Pair(E,M) E+M-mean > 0.968 (reclaim from king -> back to 23.8%; np2 baseline 0.951, gap 0.017) + LOTTERY Overall total > 0.728. Defends Single-M (>=0.849). NOTE: this draw rolled django-11551 as a break -> likely a Pair(E,M) MISS (recovering on total); if so it's a spent draw -> draw again for a clean django roll. 1 of ~4 np2 + 1 np3 planned. m12/np2/np3/m26 LIVE. solution=upload_miner_np2.py (byte-identical)." }
  uphardsub: { hotkey: 5FXAR4pqoo6n4CjoyFA9kaYTBczkZoHDr9Fwf4Qt5tP2gMZZ, label: uphard_nocap, version: np2_combined_keep60_floor16k_NOCAP, competition: 108, key: "its OWN DeepInfra+Venice account; vet with scripts/vet_draw.py at scored.", sha256: "96052567 (LIVE bytes); on-disk recon upload_miner_uphard_nocap.py = fb577c10 (logic-identical, comments differ)", uploaded: "2026-06-29 by USER — the 'previous' uphard, PRE-max_keep", status: "SCORED 2026-06-30 = 0.416 FAILED (E0.984/M0.266/H0.032; 17% breaks incl django-12039 -2.389 / django-12774 -2.204 / sympy-23262 -1.906 = the PREDICTED uncapped-tail crater on big baseline-pass contexts) -> DISCARD (own hotkey, untouched). De-risked sibling uphardcap32sub (32k cap) would have avoided it. [orig design:] keeps HUGE results UNCAPPED (60%, 160k->76k) -> np5 crater RISK (CONFIRMED). ⚠️ edited-in-place mistake: exact uploaded bytes overwritten + untracked -> NOT byte-recoverable; uphard_nocap.py is the logic-identical reconstruction.", notes: "solution=upload_miner_uphard_nocap.py (reconstruction; live=96052567)." }
  uphardcap32sub: { hotkey: TBD-USER-ASSIGNS-OWN-DEEPINFRA+VENICE-ACCOUNT, label: uphard_cap32, version: np2_combined_keep60_floor16k_maxkeep32k_OVERALL, competition: 108, key: "its OWN isolated DeepInfra+Venice account; vet with scripts/vet_draw.py. OWN hotkey -> cannot hurt the np2/np3 live floor.", sha256: b60c7d68, uploaded: "NOT YET — READY; de-risked sibling of the live nocap version", status: "READY. COMBINED-OVERALL contender, DE-RISKED: caps HUGE results at 32k (np3's PROVEN Hard zone) vs nocap's uncapped 60% (160k->32k vs 76k). Differs from the live nocap ONLY on >53k results. Codex GO'd the proportional mechanism; floor-16k + max_keep-32k = strict tightenings (re-verified cache-stable/idempotent/bounded/compliant). Upload on a 2nd clean account = best-of-2 vs the live nocap. READ at scored: vet_draw.py -> Easy>=0.85 -> OVERALL vs 0.728 (PRIZE) + Pair(E,H) vs 0.760.", notes: "cap = min(32000, max(16000, 0.60*len)): <=16k pass(=np2) | 16-26.7k cap16k(=np2 -> E1.052/M0.849) | 26.7-53k keep0.60 | >53k cap32k(~np3). PREDICTED E~1.052/M~0.849/H~0.37 -> Overall ~0.76 > king 0.728 (takes the 57% crown). UNPROVEN: Hard-capture (platform decides). FALLBACK if Hard<0.369: np3. solution=upload_miner_uphard_cap32.py" }
  uphardomitcountsub: { hotkey: DORMANT-DO-NOT-UPLOAD-UNTIL-README, label: uphard_omitcount, version: np2_combined_cap32_PLUS_informative_omitcount_GATED, competition: 108, key: "DORMANT — not for upload yet.", sha256: 9a02818f, uploaded: "NOT YET — PRE-BUILT + DORMANT (OMIT_COUNT_ENABLED=False -> byte-behaviorally = cap32 = compliant; scanner PASS).", status: "GATED / PENDING README. = uphard_cap32 + Karim's INFORMATIVE omitted-count annotation inside [[CMP]] blocks ('{n} chars omitted'), behind OMIT_COUNT_ENABLED flag (default False = dormant/compliant). Karim proposed it Discord 2026-06-29; Matt|SOMA 'looks okay at first glance' (informative-only, his favored category) BUT NOT yet in README_prompting.md -> emitting it now = FAIL REVIEW. ACTIVATE ONLY AFTER: (1) `python scripts/check_readme_current.py` shows the omit-marker landed, (2) set OMIT_COUNT_TEMPLATE to the README's EXACT string, (3) OMIT_COUNT_ENABLED=True, (4) scanner + Codex re-audit. WHY: telling Qwen3 content was truncated -> fewer breaks / better Hard flips (aids combined-Overall).", notes: "solution=upload_miner_uphard_omitcount.py (DORMANT). Standing gate: scripts/check_readme_current.py before every hotkey." }
  uphardsaliencesub: { hotkey: 5DAbJikTa2iEQgeAU7RjFMrMDm6rNFRUYnverTK4aG94B7Lk, label: uphard_salience, version: np2_combined_cap32_PLUS_salience_pin_imports_structure, competition: 108, key: "its OWN isolated DeepInfra+Venice account; vet with scripts/vet_draw.py. OWN hotkey -> cannot hurt the np2/np3 live floor.", sha256: "695b4fe4 (sha256, VERIFIED CORRECT 2026-07-01 — the 06-30 'STALE' note was MY hash-algo confusion: 695b4fe4 is sha256, 57e325c7 is git-hash, efd7b923 is SHA-1, ALL of the same current bytes). FINAL UPLOAD SET v1/3, UPLOADED 2026-06-30 → 5DAbJik.", uploaded: "NOT YET — BUILT + Codex GO-WITH-CHANGES (fix applied: raise\\s+\\w -> raise\\b, re-verified). READY for spare-hotkey test.", status: "CODEX GO (change applied). THE FIRST EXTRACTION-SALIENCE fix (every prior miner only tuned the cap). = cap32 + pin code skeleton (imports/decorators/raise/except) via _STRUCT_PATTERN, so the extractive stops dropping short structural lines (it scored them low on token-richness). ROOT CAUSE (reports/reanalysis_2026-06-29.md): on code file-reads the base dropped imports -> agent loses file API -> NameError/ImportError -> BREAK (verified: real 32k sympy read dropped 19/30 imports). VERIFIED: salience keeps 30/30 imports vs cap32's 11/30 at ~identical 1.98x (cost ~33 chars); scanner PASS; deterministic/idempotent. Targets our higher break rate (14% vs king 10%), esp. import-heavy sympy Hard tasks. CAVEAT: break-causation is a strong hypothesis, platform decides the magnitude.", notes: "diff-vs-cap32 = _STRUCT_PATTERN regex + 1 pin-check line. Read at scored: break rate vs cap32/np2 (should DROP) + OVERALL/Pair(E,M)/Hard. solution=upload_miner_uphard_salience.py" }
  uphardsaliencecap64sub: { hotkey: TBD-USER-ASSIGNS-OWN-DEEPINFRA+VENICE-ACCOUNT, label: uphard_salience_cap64, version: salience_PLUS_cap64_ceiling, competition: 108, key: "its OWN isolated DeepInfra+Venice account; vet with scripts/vet_draw.py. OWN hotkey -> cannot hurt the np2/np3 live floor.", sha256: "9f204050 (git-hash). FINAL UPLOAD SET v3/3 2026-06-30.", uploaded: "NOT YET — BUILT. = salience with NP_MAX_KEEP 32k->64k (the ONLY diff). Codex-clean diff (differs from approved salience only in the constant).", status: "READY. MIDDLE/looser cap. Keeps big results fuller than cap32 (caps the keep at 64k vs 32k); diverges from nocap+pin ONLY on results >107k chars (=0.60*len>64k) so it is NEARLY a duplicate of nocap in practice. ⚠️ STRATEGY RISK (claude+codex review): Hard peaks ~28-32k (np3 H0.369) and CRATERS looser (nocap H0.032) -> cap64 may LOWER the Hard we need for Overall. Upload tests the H-peak hypothesis directly. Read at scored: vet_draw.py -> Hard vs salience/np3 (does looser cap crater H?) + OVERALL vs 0.703.", notes: "solution=upload_miner_uphard_salience_cap64.py. ⚠️ DEPRECATED for Overall (Claude+Codex 2026-06-30 18:00Z): cap64 ~= nocap (diverges only >107k chars) -> shares nocap's best-case 0.622 < 0.703 -> SUPERSEDED by np3pinsub/np2pinsub." }
  uphardsaliencenocapsub: { hotkey: TBD-USER-ASSIGNS-OWN-DEEPINFRA+VENICE-ACCOUNT, label: uphard_salience_nocap, version: salience_PLUS_uncapped_keep, competition: 108, key: "its OWN isolated DeepInfra+Venice account; vet with scripts/vet_draw.py. OWN hotkey -> cannot hurt the np2/np3 live floor.", sha256: "0263541e (git-hash). FINAL UPLOAD SET v2/3 2026-06-30.", uploaded: "NOT YET — BUILT. = salience with NP_MAX_KEEP 32k->1e9 (UNCAPPED; the ONLY diff). Codex-clean diff.", status: "READY. HIGHEST ceiling, HIGHEST risk = uncapped keep + import-pin. Directly tests whether the live nocap's 0.416 crater (M0.266 17% breaks, H0.032) was DROPPED-IMPORTS (fixable by the pin) or BULK/TAIL (not). ⚠️ if nocap's breaks weren't import-caused, this craters again. Read at scored: vet_draw.py -> break% vs live nocap (should DROP if pin works) + M-recovery + Hard + OVERALL vs 0.703.", notes: "solution=upload_miner_uphard_salience_nocap.py. Live no-pin sibling = uphardsub 5FXAR4 scored 0.416. ⚠️ DEPRECATED for Overall (Claude+Codex reconciliation 2026-06-30 18:00Z): best-case mean 0.622 < 0.703 (Hard cratered 0.032, pin fixes M not H) -> SUPERSEDED by np3pinsub/np2pinsub as shots 2-3." }
  np3pinsub: { hotkey: 5E4Y4jzYKY75bksLUK3VtArvt3tpX4KH1bv2BqNMHHQnuqa5, label: np3_pin, version: np3_flat28k_PLUS_salience_pin, competition: 108, key: "its OWN isolated DeepInfra+Venice account; vet with scripts/vet_draw.py. OWN hotkey -> cannot hurt the np2/np3 live floor.", sha256: "489f4e3c (sha256; git-hash 5ecc8e9e). FINAL UPLOAD SET Shot 2/3, UPLOADED 2026-06-30 → 5E4Y4jz. BASE VERIFIED: on-disk upload_miner_np3.py sha256 420de1cc == registry live-np3sub sha ⇒ np3_pin truly = LIVE np3 + only-the-pin.", uploaded: "NOT YET — BUILT. = LIVE np3 (flat 28k, scored E0.859/M0.828/H0.369 mean0.685) + the EXACT salience _STRUCT_PATTERN pin (diff-vs-np3 = only the pin; regex byte-identical to approved salience). Compliance/cache/bound INHERITED (np3 live-compliant + already-audited pin + verified-minimal diff).", status: "READY. The GROUNDED Hard shot: np3 has our best PROVEN Hard (0.369 = the H-peak) but weak E/M (0.859/0.828); the import-pin aims to lift its E/M breaks -> push 0.685 over 0.703 Overall. Read at scored: vet_draw.py -> E/M vs np3 (should RISE) + Hard holds 0.369 + OVERALL vs 0.703.", notes: "solution=upload_miner_np3_pin.py. Base = upload_miner_np3.py (live 5F9ZRe)." }
  np2pinsub: { hotkey: 5FLUziw5Cpny4D5obqvi72QJk5zhmH3njow6jWxe91eJLqeQ, label: np2_pin, version: np2_flat16k_PLUS_salience_pin, competition: 108, key: "its OWN isolated DeepInfra+Venice account; vet with scripts/vet_draw.py. OWN hotkey -> cannot hurt the np2/np3 live floor.", sha256: "54443001 (sha256; git-hash 24352456). FINAL UPLOAD SET Shot 3/3, UPLOADED 2026-06-30 → 5FLUziw. BASE VERIFIED: on-disk upload_miner_np2.py sha256 a64231c9 == registry live-np2sub sha ⇒ np2_pin truly = LIVE np2 + only-the-pin.", uploaded: "NOT YET — BUILT. = LIVE np2 (flat 16k, scored E1.052/M0.849/H0.173 mean0.691) + the EXACT salience _STRUCT_PATTERN pin (diff-vs-np2 = only the pin). Compliance/cache/bound INHERITED.", status: "READY. The SAFEST nudge: np2 already at 0.691 mean (nearest 0.703); the pin reduces its baseline-pass breaks -> small E/M lift could clear 0.703. Lowest ceiling of the 3 (np2's H0.173 is low) but lowest risk. Read at scored: vet_draw.py -> break% DOWN vs np2 + OVERALL vs 0.703.", notes: "solution=upload_miner_np2_pin.py. Base = upload_miner_np2.py (live 5CPbtf)." }
  m33sub: { hotkey: 5CM1JKAsPQvMnD9NMEAGV9ZZW44ifC3mDN3T5AfqGLCrXKqD, label: m33, version: np2_clone_overall_variance_draw, competition: 108, key: "984d (sk-or-...984d) — SAME DeepInfra-pinned account as np2/np3/m26 (probe-verified 100% DeepInfra). Spare key (usage 0 at probe).", sha256: a64231c9, uploaded: "2026-06-28 ~15:56 UTC (after registering the hotkey on SN114 — first attempt 403'd 'not registered in metagraph'; uploads still OPEN)", status: "SCORED 0.216 POISONED by the DeepInfra-ONLY-pin TRUNCATION (NOT variance/algo). 9 pin-truncated tasks -1.41/5 breaks; 41 post-fix full-token tasks scored ~=np2 (1.04x tokens) = routing fix PROVEN. DISCARD as an Overall result. LESSON: DeepInfra-only = no fallback = truncation craters -> use DeepInfra+Venice.", notes: "CLEAN OVERALL VARIANCE DRAW (the precise lottery): np2 already BEATS #1 on Medium (0.849 vs 0.719) + ~ties Easy, so it needs only Hard>=0.189 (+0.015 vs np2's 0.173, WITHIN run-variance) to top #1's 0.697 and WIN the 57% Overall element. Distinct hotkey = best-of-N (Codex-confirmed). Binding now CLEAN (DeepInfra-pinned+funded) so the np2b/np2c crater risk is gone. ADDITIVE: np2/np3/m26 hold 23.8% regardless. solution=upload_miner_np2.py (byte-identical)." }
  np3redraw1sub: { hotkey: 5DSci2Zqquaf9DStmgNrrC5kETP95LybncgW6XaKbQn8pq25, label: np3_redraw1, version: np3_byte_identical_variance_draw, competition: 108, key: "own account (clean: vet_draw.py cache 94.6%, 0 timeouts).", sha256: "420de1cc (np3 logic)", uploaded: "2026-06-29 by USER", status: "SCORED 0.464 = DISCARDED (BAD DRAW, not bad account). vet_draw.py: cache 94.6% clean / 0 timeouts BUT break 18.8% (E/M 19%) vs live-np3 clean 11.2%. E0.615/M0.462/H0.324 vs np3 0.682. Damage on MEDIUM (django-11292 broke 4/5 — np3 passes it clean +1.50; 12039 worse); Easy/Hard ~held. = np2d1 pattern for np3: byte-identical + clean account + unlucky roll. DATA POINT: 2nd clean redraw landing ~0.46 (np2d1 0.49) -> our typical clean draw ~0.46-0.49, live 0.68 = favorable tail. DISCARD.", notes: "discarded variance data point; solution=np3 (upload_miner_np3.py)." }
  np_propsub: { hotkey: 5FNrjmdT2Kevx1nQYCs1DBnkskduJ4eNPx7489NFBRAtQYQU, label: np_prop, version: np_prop_hybrid_king_method_cap16k_ceil2x, competition: 108, key: "f47b (sk-or-...f47b) — DeepInfra-PINNED (user restricted account to DeepInfra-only 2026-06-28; probe-verified 100% DeepInfra seq+16-concurrent, 0 errors/429). SAME shared account as LIVE np2/np3/m26 (USER-confirmed) — ⚠️ ~$51 pool at upload (granted 847/used 796.05); WATCHING balance for drain.", sha256: 05bca9f2, uploaded: "2026-06-28 ~06:1x UTC", status: "UPLOADED via cmd#2 (upload_miner_with_openrouter_key.py, wallet tony-miner). Registration lag — NOT on board at 06:21Z (np2c took ~2h). READ ONLY AT status=scored AND verify Easy~1.05 (binding) FIRST (Easy<0.85 => starved => IGNORE, like np2c/np5).", notes: "THE HYBRID = the KING'S METHOD (§24/§26; Codex pre-upload GO agent a1a413e; sha 05bca9f2 re-verified at upload). = np2 EXACT for tool-results <=32k (E/M BYTE-IDENTICAL to np2 -> our 14.3% Pair(E,M)+Single-M PROTECTED) + a 2x compression CEILING on >32k (NP_MIN_KEEP_FRAC=0.5: 64k->32k=50% vs np2's flat 16k/4x = the king's never-over-compress). TARGETS Hard (fix np2's H0.173 over-compression breaks on big blocks) -> Overall(57%). ADDITIVE: np2/np3/m26 hold 23.8% regardless. AT scored (AFTER Easy-binding check): Hard vs np2 0.173 (the fix) + E/M held (>=np2, must NOT regress) + BREAK count vs np3 (Codex watch: breaks climb on >32k -> tune ceiling toward 28k/np3). RISK: marginal wander on the 32k keep (between np3-safe 28k=0.682 and np5-crater 48k=0.25). ⚠️ COST WATCH: shares the ~$51 account with LIVE income; screening cheap, but full eval (status=evaluating) is the spend spike -> TOP UP at that transition. solution=upload_miner_np_prop.py" }
  np5sub: { hotkey: 5CZxaU17TCfy3rVrmG3T3kqZkLUMELFMBAcXLEAq3aTezmUN, label: np5, version: np5_near_passthrough_cap48k, competition: 108, key: "47b/good account (user-confirmed same provider as np2; per-task identical-treatment tasks np5~np2 -> NOT a provider crater).", sha256: 62732c22, uploaded: "2026-06-27 ~21:xx UTC", status: "SCORED 0.25 = CRATER (E0.420/M0.247/H0.124). The 48k NEAR-PASSTHROUGH CLIFF: keeping the 16-48k results RAW -> catastrophic agent WANDER (np3 28k=0.682 -> np5 48k=0.25 cliff). CONFIRMS near-passthrough craters for us -> Bet 1 DEAD, cap SETTLED at 16k. DEAD hotkey.", notes: "Bet 1 = near-pure-passthrough Overall challenger (cap 16k->48k). FAILED: per-task proved np5~np2 on IDENTICAL-treatment (small-result) tasks => same good provider (user right, my provider hypothesis WRONG); the crater is the CAP (big-result divergence -> wander), not the provider. #1's near-passthrough edge NOT replicable via our cap (we crater when light). solution=upload_miner_np5.py" }
  np2csub: { hotkey: 5GCWMTZk6u8ziHHQ9g5HBLxQ2o659mjCiXjX6JH5zr9G5YoU, label: np2c, version: np2_clone_variance_lottery2, competition: 108, key: "47b key (USER's best STABLE / good-account key) + prefer-DeepInfra routing. VERIFY Easy~1.05 at scored = binding OK (np2b/np5 cratered on BAD provider-binding; np2c uses the proven-stable key to avoid that).", sha256: a64231c9, uploaded: "2026-06-28 ~02:41 UTC", status: "UPLOADED — registration lag/screening. READ ONLY AT status=scored AND ONLY AFTER verifying Easy~1.05 (Easy<0.85 => binding failed => IGNORE, like np5). Byte-identical np2 (sha a64231c9, diff=0).", notes: "VARIANCE LOTTERY #2 for the OVERALL element (57%). Exact np2 (0.684) re-draw on a FRESH hotkey + the 47b STABLE key (good DeepInfra account, low-cache providers ignored) -> independent eval draw; if it tops #1 5DCnA57 (0.697) it WINS Overall. Gap is only 0.013 = within the run-variance (§15-E), so a real-but-uncertain shot. ADDITIVE & SAFE: np2(5CPbtf)+np3(5F9ZRe)+m26 hold 23.8% regardless of np2c. CONTRAST: np2b (5DG31B, e2c/AtlasCloud acct) cratered to 0.13 = provider-confound; np2c fixes it with the 47b stable key + DeepInfra routing -> a CLEAN draw this time. Provider-binding has bitten m14/np2b/np5, so the Easy~1.05 check is mandatory before reading. m12 (5Dz7) LIVE/untouched. solution=upload_miner_np2c.py (= np2 byte-identical)." }
  np2bsub: { hotkey: 5DG31BojWwvC43DXZuxAn9XLKDto1P5aRwbaf6oaTrENJ7Eh, label: np2b, version: np2_clone_variance_lottery, competition: 108, key: "⚠️ DIFFERENT OpenRouter ACCOUNT than np2/m12 (USER chose 2026-06-27, testing whether the m14 account-confound was a FALSE analysis). NOT the same-account discipline — intentional.", sha256: a64231c9, uploaded: "2026-06-27 ~10:41 UTC", status: "SCORED 0.129 (E0.565/M-0.400/H0.249) vs np2 0.684 = DELTA -0.555 on BYTE-IDENTICAL code. CONFOUND CONFIRMED REAL: the OpenRouter PROVIDER (AtlasCloud NOT blocked on this account) swings the score ~0.55. DEAD as a lottery ticket (confounded crater, wins nothing). KEEPS the same-provider discipline. KEY POSITIVE: provider routing = the biggest lever found (non-compression) — see state/CURRENT.md top.", notes: "DUAL PURPOSE. (1) VARIANCE LOTTERY for the Overall element (57%): np2 base 0.684, gap to #1 (5DCnA57 0.697) = 0.013 = within the run-variance we measured (§15-E) -> a fresh independent draw has a real shot at >0.697 -> win Overall. (2) NATURAL EXPERIMENT on the m14 account-confound: m14 changed BOTH code+account (confound INFERRED not proven, false 0.176); np2b changes ONLY the account (same np2 code) -> isolates the account variable cleanly. READING at scored: np2b ~0.684 => m14 'confound' was FALSE (account-independent) + it's a LIVE Overall ticket; np2b <<0.684 (m14-like ~0.18) => confound REAL, this draw is biased-LOW (discard as a lottery result, account matters, revert to same-account discipline). ADDITIVE & SAFE: m12 + np2(5CPbtf) + np3(5F9ZRe) all on the ORIGINAL account, hold 23.8%, untouched regardless of np2b. solution=upload_miner_np2.py" }
  np3sub: { hotkey: 5F9ZReArwp25LaG1D8cze9WgWEeo8uofCjbsLTKaJk5ft7Jp, label: np3, version: np3_near_passthrough_cap28k, competition: 108, key: "SEPARATE hotkey (5F9ZRe); SHOULD be SAME OpenRouter account as m12/m26/np2 — CONFIRM (m14 confound)", sha256: 420de1cc, uploaded: "2026-06-27 ~02:21 UTC", status: "UPLOADED — IN QUEUE. READ ONLY WHEN status=scored. ADDITIVE Hard bet: np2 stays LIVE holding our 14.3% (Pair(E,M)+Single-M); np3 tries for OVERALL (57%) by keeping big UNIQUE blocks fuller.", notes: "np2 + NP_RESULT_CAP 16k->28k. DIAGNOSIS (repetition analysis 6 comp-108 trajs): Hard breaks from big UNIQUE blocks (24k-64k; 4/6 tasks >16k) np2's 16k cap shreds, NOT repeats (exact-dup ~10% = dedup weak). 28k keeps 24k blocks FULL + 56-64k to 28k -> fewer Hard over-compression breaks -> +Hard -> Overall (np2 needs only +~0.05 Hard, Medium cushion +0.130 over king). Codex audit GO (aabdb48e): integrity CONFIRM (only the cap; rewriter-bug-free; harnesses re-run 99.8% prefix-stable/0 orphans/idempotent/<=native/24k passes full,56-64k->28k), invariants CONFIRM, strategy CONFIRM-the-bet-with-real-risk. RISKS (platform-only): more near-passthrough -> wander could drop E/M; some Hard agent-side (ceiling ~0.34); if a big block is mostly PINNED error/traceback the cap won't engage -> np3==np2 -> Hard flat + E/M wander for nothing. GATE on scored: Overall vs 5DCnA57 0.703 + Hard vs np2's 0.173 + E/M held (don't regress below np2). CONTINGENCY: E/M erodes -> tune cap to 24k; Hard flat -> np4 better extractive on the 56-64k blocks. m12 (5Dz7) LIVE, np2 (5CPbtf) LIVE = our 14.3%. solution=upload_miner_np3.py" }
  m17sub: { hotkey: 5FqAHka1xB3NHeg4JKiXwcTEmAAxQuA1azNdWpKtfWB5RtC6, label: m17, version: m12_extractive_harvest, competition: 108, key: "same OpenRouter account as m12", sha256: ac9519f0, uploaded: "2026-06-25 ~02:3x UTC", status: "NOT QUALIFIED — screener_passed=false. Failed the screener gate: django-11551 (baseline-passing/Easy screener task) broke 2/5 [-4,-4,1.2,1.0,1.3]. Likely RUN-VARIANCE (m17 keeps MORE content so should not break more; m12 breaks 11551 ~1/5 too; 2/5 vs 1/5 within noise) but the screener DQd it. Probe DEAD (no full eval). LESSON: breaking a baseline-passing screener task = disqualification, not just score drag.", notes: "m12 + HARVEST blind-truncation -> extractive-then-cap (_select_then_cap pins error/test/diff/path/sig lines buried mid-result). Phase-A safety PASS: 57/58 fns byte-identical to m12, only compress_structurally + _select_then_cap differ; compress_gently/extractive_compress/handle_assemble/state byte-identical; RICH output byte-identical to m12 (Hard-safe); deterministic; harvest bytes<=m12 +0.6%; DEMONSTRATED keeps buried FAILED/AssertionError line m12 drops. ZERO-downside PROBE (cannot dent Hard/ratio by construction); addresses ~1-2 of 10 break tasks (truncation cases, NOT whole-drop). READ ONLY WHEN scored; wont beat king; signal = does it move the BOARD vs m12 0.768. m12 (5Dz7) stays LIVE. solution=upload_miner_m17.py" }
  m16sub: { hotkey: 5DADzDbZb3y6, label: m20b-v2, version: m20b_v2_superseded_view_dedup, competition: 108, key: "sk-or-v1-b94dc..facbf8 (SAME OpenRouter account as m12 — user-confirmed, no backend confound)", sha256: 9bb8a01c, uploaded: "2026-06-24 ~21:0x UTC", status: "EVALUATING (2026-06-24 ~21:5x) — PASSED screener ✓ (review-clean/compliant); Easy(eval)=1.006, Hard/Medium=N/A pending. NOT scored — Hard is the danger (5DAh: 0.853E eval -> -3.6H scored -> 0.051). Dashboard now has a Screener column; Overall during screening==Easy-only screen score.", notes: "m12 + superseded-file-view BLIND collapse + recency guard (keep 2 newest views) + frequency gate (>=3 reads). Offline: compliant PASS, deterministic, real ratio gain. THESIS: raise per-task ratio bonus on passing tasks (esp M+H) via redundant-view removal -> lift Overall toward #1. Local m12-vs-m20bv2 RUNS=5 A/B running in parallel as cross-check. READ ONLY WHEN status=scored (ignore evaluating - 5DAh showed 0.887 eval->0.051 scored). ACCEPT-direction iff >= m12 with M/H intact; watch savings-multiplier/wander (m20b A/B showed +tokens). m12 (5Dz7) stays LIVE regardless. solution=upload_miner_m20b_v2.py" }

# ---- other historical hotkeys (kept so legacy tooling still flags them 'ours') ----
historical:
  m1:  { hotkey: 5CmLSfa3BT79PSn5JyddzLBimd8UeJZ9uz7k2qoCsAn37neq, version: v2,  notes: "early, weak (0.884)" }
  m2:  { hotkey: 5FCRm5fGMYtiii42ProB2cTGoYdYkj7Navn1FPN3ZoP3YpyX, version: v4,  notes: "1.003" }
  m3:  { hotkey: 5Cqek9Ddsmxa1kPL3DZ91VbWYGAZKScoRrUN62jNF7GSofTZ, version: v6,  notes: "aggressive harvest (1.080)" }
  m5:  { hotkey: 5EP1eurVw3QWQX617GMt12cmZ1E4RV948Lyjsz1UbDrAKEj6, version: v7,  notes: "broken/negative (-2.507)" }
  m6:  { hotkey: 5GKM3GACUxC87qn6VecDGASrNyYDKmJRX5hLDsBaXtdCGvPF, version: v8,  notes: "1.034" }
  m12: { hotkey: 5Dz7JaCBw6t9ktdRVaLYi3ntEzvCDfb5XmTHyzPdDyT6KS9j, version: v26, notes: "depth-gated; built+validated, NEVER registered (404 on dashboard)" }
