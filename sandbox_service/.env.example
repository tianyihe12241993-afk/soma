
# Service Configuration
SANDBOX_SERVICE_HOST=0.0.0.0
SANDBOX_SERVICE_PORT=8001

# Platform callback for benchmark reports
COMPACT_BENCH_REPORT_URL=http://sandbox_private_url:port
COMPACT_BENCH_REPORT_TIMEOUT_SECONDS=15

# sandbox_service installs SOMA-benchmark into its own Python environment and
# mirrors the SOMA-plugin template from GitHub.
COMPACT_BENCH_BENCHMARK_PACKAGE_SPEC=git+https://github.com/DendriteHQ/SOMA-benchmark.git
COMPACT_BENCH_PLUGIN_REPOSITORY_URL=https://github.com/DendriteHQ/SOMA-plugin.git

# Optional upstream LLM/OpenAI-compatible base URL.
# For OpenClaw runs, sandbox_service keeps one shared nginx Docker proxy and one private
# internal Docker network for benchmark containers.
# Copilot runs bypass sandbox nginx and use this upstream URL directly (Copilot has its own benchmark-side proxy/middleware path).
# For OpenClaw's OpenAI-compatible HTTP API, include the /v1 prefix.
COMPACT_BENCH_LLM_BASE_URL=
COMPACT_BENCH_LLM_PROXY_IMAGE=nginx:1.27-alpine
COMPACT_BENCH_LLM_PROXY_PORT=8080
COMPACT_BENCH_PRIVATE_NETWORK_NAME=soma-benchmark-private

# Copilot shared proxy lifecycle: when enabled, sandbox_service keeps one Copilot proxy
# compose stack alive across multiple benchmark-solve subprocess runs.
COMPACT_BENCH_COPILOT_SHARED_PROXY=true
COMPACT_BENCH_COPILOT_COMPOSE_PROJECT=soma-copilot-sandbox-shared
# Optional explicit compose file path; if omitted, resolves from installed soma_bench package.
# COMPACT_BENCH_COPILOT_COMPOSE_FILE=/absolute/path/to/SOMA-benchmark/src/soma_bench/benchmark/backends/copilot/copilot-cli-container/docker-compose.yml
# Per-run compression containers are named compression-run-<run_id> and proxy routes by this template.
COMPACT_BENCH_COPILOT_COMPRESSION_URL_TEMPLATE=http://compression-run-{run_id}:8000/

# Callback reliability / retries
COMPACT_BENCH_CALLBACK_QUEUE_PATH=/var/lib/soma-sandbox/callback_queue.sqlite3
COMPACT_BENCH_CALLBACK_RETRY_POLL_SECONDS=1
COMPACT_BENCH_CALLBACK_RETRY_BASE_SECONDS=2
COMPACT_BENCH_CALLBACK_RETRY_MAX_SECONDS=300

# Capacity flood protection
COMPACT_BENCH_CAPACITY_COOLDOWN_SECONDS=2

# nginx proxy timeout/keepalive tuning
COMPACT_BENCH_NGINX_PROXY_CONNECT_TIMEOUT_SECONDS=30
COMPACT_BENCH_NGINX_PROXY_SEND_TIMEOUT_SECONDS=1800
COMPACT_BENCH_NGINX_PROXY_READ_TIMEOUT_SECONDS=1800
COMPACT_BENCH_NGINX_PROXY_KEEPALIVE_TIMEOUT_SECONDS=75

# OpenClaw WS watchdog (retry only GatewayTransportError 1006)
SOMA_OPENCLAW_WS_WATCHDOG_RETRIES=1
SOMA_OPENCLAW_WS_WATCHDOG_BACKOFF_SECONDS=1
